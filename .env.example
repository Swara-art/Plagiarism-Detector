# PostgreSQL is mandatory; no in-memory or demo-user fallback is used.
DATABASE_URL=postgresql+psycopg://postgres:change-me@localhost:5432/plagiarism_detector
# Generate a long random value, for example: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=replace-with-a-long-random-secret
